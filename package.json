{
  "scripts": {
    "firebase": "cd firebase && firebase emulators:start --import mock-data",
    "function": "cd firebase/functions && npm run watch",
    "frontend": "cd frontend && npm run dev",
    "export": "cd firebase && firebase emulators:export mock-data"
  }
}
